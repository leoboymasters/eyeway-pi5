# Eyeway App - Raspberry Pi 5 Requirements
# Optimized for ARM64 architecture with CPU-only inference

# Core
numpy>=1.24.0
scipy>=1.10.0

# Computer Vision
opencv-python-headless  # Headless version for Pi
pillow>=9.0.0

# Machine Learning (CPU-only)
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu

# YOLO Object Detection
ultralytics>=8.0.0

# Depth Anything V3
# Install from local source:
#   pip install ./depth-anything-3
# Required dependencies for DA3:
timm>=0.9.0
transformers>=4.30.0
huggingface-hub>=0.20.0
omegaconf>=2.3.0
einops>=0.7.0
safetensors>=0.4.0
imageio>=2.31.0

# GUI (optional - for display mode)
# customtkinter>=5.0.0  # Uncomment if using GUI

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# Audio feedback (optional)
# pyttsx3  # Text-to-speech for audio alerts
